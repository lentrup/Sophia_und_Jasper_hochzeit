run.config:
  engine: ruby
  engine.config:
    runtime: ruby-2.3
  cache_dirs:
    - vendor
  extra_packages:
    - nodejs
    - node_modules
    - pkgconf
    - libxml2
    - libxslt
    - yarn
    - nginx

data.db:
  image: nanobox/postgresql:9.5
